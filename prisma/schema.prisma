generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model Mahasiswa {
  id   Int    @id @default(autoincrement())
  nama String
  nim  String
}